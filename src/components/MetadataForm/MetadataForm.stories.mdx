import { Meta, Story, Canvas } from '@storybook/addon-docs'
import MetadataForm from './MetadataForm'
import schema from './schema.json'
import uiSchema from './uiSchema.json'

<Meta
  title="Metadata Form"
  component={MetadataForm}
  decorators={[
    (MetadataForm) => (
      <div
        style={{
          padding: '40px 10px',
        }}
      >
        <MetadataForm />
      </div>
    ),
  ]}
/>

# Metadata Form

A [JSONForms](https://jsonforms.io/) form generated from JSON schema which can be passed as a prop.

## Default

<Canvas>
	<Story name="Default">
		<MetadataForm schema={schema} uischema={uiSchema} data={{
			documentDescription: '__DOCUMENT_DESCRIPTION__',
		}} renderers={[]} />
	</Story>
</Canvas>

## No data

<Canvas>
	<Story name="No data">
		<MetadataForm schema={schema} uischema={uiSchema} data={{
			documentDescription: ''
		}} renderers={[]} />
	</Story>
</Canvas>
